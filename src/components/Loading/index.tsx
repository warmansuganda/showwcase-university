import { Wrapper, Animation, LoadingText } from "./LoadingStyles";

function Loading() {
  return (
    <Wrapper>
      <Animation />
      <LoadingText>Loading...</LoadingText>
    </Wrapper>
  );
}

export default Loading;
